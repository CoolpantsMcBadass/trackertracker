TrackerTracker Changelog
========================

[1.0.5] 2026-06-02
- background.js: block-all rules now use excludedInitiatorDomains: tracker.domains (all domains for that tracker) instead of just the single blocked domain; fixes sites like Reddit where the initiator (reddit.com) differs from the blocked resource domain (redditstatic.com, redditmedia.com)
- manifest.json, store-submission/store-listing.txt: updated description to "See exactly what is tracking you and why"

[1.0.4] 2026-06-02
- background.js: excluded performance and other categories from block-all in addition to support; performance covers CDN/infrastructure (Akamai, Azure CDN, CloudFront), other covers broad platform domains like office.com whose ||office.com DNR rule was breaking Outlook Web
- background.js: added priority-4 site-level allow rules for disabled sites when block-all is active, so per-site disable properly overrides block-all

[1.0.3] 2026-06-01
- overlay.js: switchTab("page") now calls renderTrackers() on every switch; previously switching from settings back to page tab showed stale DOM from before blockAllActive was set, so trackers didn't display as blocked even when block-all was on

[1.0.2] 2026-06-01
- background.js: removed blockAllEnabled from chrome.storage - it was being restored on every SW restart, making block-all re-activate on page load even without "block by default" being set; block-all is now session-only in memory, only blockAllDefault persists to storage
- background.js: init() now only calls enableBlockAll() if blockAllDefault is true; otherwise clears any leftover block-all DNR rules from the previous session (fixes "blocking by default without clicking block by default" and the cascading "can't unblock" issue caused by blockAllActive being incorrectly true)
- background.js: BLOCK_TRACKER and UNBLOCK_TRACKER handlers now await initPromise so trackerList and blockedTrackers are fully restored before acting

[1.0.1] 2026-06-01
- background.js: fixed race condition where GET_BLOCK_ALL and SET_BLOCK_ALL messages could arrive before init() finished loading trackers and restoring storage state; GET_BLOCK_ALL now awaits initPromise before reading blockAllEnabled/blockAllDefault, SET_BLOCK_ALL awaits initPromise before calling enableBlockAll() so trackerList is always populated
- background.js: removed duplicate init calls (onInstalled + onStartup listeners were redundant in MV3 since the SW always runs module-level code on start); replaced with single const initPromise = init()
- background.js: added try/catch in enableBlockAll around updateDynamicRules so Chrome API errors surface to the console instead of silently swallowing
- background.js: SET_BLOCK_ALL now catches enableBlockAll/disableBlockAll errors and sends ok:false response
- overlay.js: added blockAllActive state variable; enableBlockAll adds DNR rules but never populated blockedSet so nothing showed as blocked in the UI; fixed by factoring blockAllActive into all blockedSet.has() checks in renderTrackers, renderBlocklist, block button click handler, and blocklist row toggle
- overlay.js: GET_BLOCK_ALL callback now sets blockAllActive and triggers re-render; blockAllGlobal change handler also updates blockAllActive and re-renders
- overlay.js: blocklist toggles are disabled when block-all is active (individual overrides are meaningless while block-all is on)

[0.8.3] 2026-06-01
- icons: final Plankton eye layout - iris centered at icon center (64,64), sclera cx=64 cy=70 rx=26 ry=23, eyebrow raised so inner bottom barely touches sclera top (y=47); re-exported at 16/48/128px
- overlay.js: inline SVGs updated to match

[0.8.2] 2026-06-01
- icons: fixed Plankton eye pupil centering - sclera made more circular (rx/ry 28/26), iris centered at same point (cx=64 cy=90), highlight adjusted; re-exported at 16/48/128px
- overlay.js: updated inline SVG to match

[0.8.1] 2026-06-01
- popup + overlay: removed inline descriptions from tracker rows - description now shows on hover only (native tooltip in popup; floating tooltip in overlay)
- popup + overlay: settings moved to gear icon (⚙) in panel header top-right; bottom nav removed from popup entirely
- popup + overlay: light/dark mode toggle restored under Settings → Appearance; popup persists to popupTheme, overlay persists to overlayTheme

[0.8.0] 2026-06-01
- popup: full redesign inspired by Bitwarden dark mode - deeper background (#15191e), bottom nav bar (This Page / Settings), tracker list grouped by category with ALL-CAPS section headers, list-row style replacing cards, full-width search bar, 360px width
- popup: settings moved into Settings tab - site toggle, block-all, block-by-default
- popup: banner status rendered as inline chip (ok/warn/none)

[0.7.4] 2026-06-01
- background.js: added preemptive block-all using sequential declarativeNetRequest rule IDs from 100000+ (avoids hash collisions with individual blocks); enableBlockAll/disableBlockAll functions
- background.js: added SET_BLOCK_ALL, GET_BLOCK_ALL, SET_BLOCK_ALL_DEFAULT message handlers
- background.js: consolidated startup into single async init() so block-all always waits for trackers to load before applying rules
- overlay.js: added "Block all trackers" and "Block by default" toggles to settings popout; state synced from background on open; turning on default auto-enables block-all and vice versa

[0.7.3] 2026-06-01
- background.js: first tracker detected on a page now pushes to the overlay immediately instead of waiting for the 400ms debounce; subsequent trackers still debounce to batch updates

[0.7.2] 2026-06-01
- data/trackers.json: generated plain-English privacy-focused descriptions for all 3435 trackers using Claude; descriptions say what each tracker collects or does to the user (not corporate blurbs)
- scripts/generate-descriptions.js: generation script retained for future re-runs if tracker list is updated

[0.7.1] 2026-06-01
- scripts/build-trackers.js: fixed CATEGORY_MAP - 'advertising' was incorrectly mapped to 'ads' instead of 'advertising', causing 1706 ad trackers to render without CSS styling or label in the popup and overlay

[0.7.0] 2026-06-01
- data/trackers.json: replaced hand-curated 241-tracker list with Ghostery Tracker Database (ghostery/trackerdb, CC-BY-NC-SA-4.0); now 3435 trackers across 5059 domains
- scripts/build-trackers.js: new build script - fetches latest @ghostery/trackerdb from npm and converts to TrackerTracker format; run with `node scripts/build-trackers.js` to update
- background.js: matchTracker() rewritten from O(n) linear scan to O(hostname_depth) suffix-based Map lookups; required for correctness at scale

[0.6.3] 2026-05-29
- background.js: toolbar badge now uses yellow bg (#f2e840) with dark text (#1a1a1a) via setBadgeTextColor; matches icon palette
- overlay.js: pill badge text changed to #e8f0ff (white-ish) in dark mode; dark (#111827) in light mode to fix yellow-on-white readability issue

[0.6.2] 2026-05-29
- overlay.js: badge redesign - removed red pill background, now shows plain number in yellow (#f2e840); zero state shows dim color only
- overlay.js: block-all toggle replaced with native checkbox (accent-color #f2e840); removed non-functional cc-toggle-track span from the label
- icons: Plankton flat-style icon (wing eyebrow touching sclera) exported at 16/48/128px; note Chrome toolbar icon requires extension reload at chrome://extensions to update

[0.6.1] 2026-05-29
- overlay.js, popup.css: swapped blue accent (#3b82f6) for Plankton eye yellow (#f2e840); darker variant updated to #c8ba00 for light mode; ties UI accent directly to icon color

[0.6.0] 2026-05-29
- icons/: replaced cookie-with-knife with Plankton's eye (green skin bg, yellow sclera, red iris, single lash) - exported at 16/48/128px via Inkscape
- overlay.js: replaced inline cookie SVG (pill + panel header) with Plankton eye SVG; full palette swap - zinc/amber → navy/blue: bg #0d1117, surface #111827, border #1e2d42, accent #3b82f6, text #e8f0ff, muted #8b9abf
- popup.css: updated :root CSS variables to match new navy/blue palette

[0.5.5] 2026-05-29
- Renamed: Cookiecutter → TrackerTracker across manifest.json, popup.html, popup.js, overlay.js, background.js; host element id updated to trackertracker-host; description updated; action title updated

[0.5.4] 2026-05-29
- overlay.js: fixed settings popout closing on inner clicks - root cause was composedPath() being pruned by closed shadow DOM for document-level listeners; replaced with two-listener strategy: document listener checks e.target !== host (detects truly outside clicks), shadow root listener uses composedPath() unpruned to close settings when clicking inside shadow but outside the popout
- overlay.js: added "Blocked only" checkbox to blocklist section, checked by default; unchecking shows all 241 trackers; search filter applies on top of blocked-only filter; toggling a tracker re-renders the list respecting current filter state; amber accent-color on native checkbox

[0.5.3] 2026-05-29
- overlay.js: fixed settings popout closing on any click inside it - root cause was the outside-click handler using e.target/.contains() which shadow DOM retargets to the host element, making every inner click look like an outside click; replaced with e.composedPath() which pierces shadow boundaries and returns the real event path

[0.5.2] 2026-05-29
- background.js: added GET_ALL_TRACKERS handler (returns full trackerList to content scripts)
- overlay.js: settings popout widened to 270px; added "Tracker blocklist" section at bottom with: filter search box, scrollable list of all 241 known trackers sorted alphabetically, per-tracker block toggle (reuses existing toggle CSS), live "N blocked" count badge, search filters by name or category; blocklist lazily fetched and cached on first settings open; toggling a tracker updates blockedSet and re-renders the page tracker list if open; light mode overrides added for all new elements

[0.5.1] 2026-05-29
- overlay.js: added "Block all" toggle in the tracker count row; checked when all detected trackers are blocked, unchecked otherwise; hidden when no trackers present; toggling ON blocks every unblocked tracker in parallel, toggling OFF unblocks all; re-renders rows when complete; light mode CSS override for label

[0.5.0] 2026-05-29
- manifest.json: added declarativeNetRequest permission
- background.js: per-tracker blocking via chrome.declarativeNetRequest.updateDynamicRules; domainToRuleId() derives stable integer rule IDs from domain strings (range 10000–59999); addBlockRules/removeBlockRules manage dynamic rules; blocked tracker names+domains persisted to chrome.storage.local as blockedTrackers; rules restored on service worker startup; new message handlers: BLOCK_TRACKER, UNBLOCK_TRACKER, GET_BLOCKED_TRACKERS
- overlay.js: added blockedSet state; each tracker row gets a ⊘ block button (hidden until hover, turns red when blocked); clicking toggles BLOCK_TRACKER/UNBLOCK_TRACKER messages; blocked rows show name strikethrough + faded logo/badge; blocked state fetched from background on init via GET_BLOCKED_TRACKERS; light mode CSS overrides for block button added

[0.4.7] 2026-05-29
- overlay.js: reverted OS-preference detection - prefers-color-scheme picks up "device default" from Chrome which resolves to light on light-mode devices, overriding the intended dark default; dark is now unconditional default when no overlayTheme is saved; user can opt into light via the settings toggle

[0.4.6] 2026-05-29
- overlay.js: theme now defaults to OS preference (prefers-color-scheme) on first load instead of hardcoded dark; saved overlayTheme still takes priority when present

[0.4.5] 2026-05-29
- overlay.js: added light/dark mode toggle in settings popout; dark is default; toggling adds/removes cc-light class on the shadow host; all overlay elements (pill, panel, header, badges, category labels, tracker logos, tooltip, settings popout) re-skin via :host(.cc-light) CSS overrides; preference persisted to chrome.storage.local as overlayTheme

[0.4.4] 2026-05-29
- overlay.js: fix gear button swallowing click - startDrag now bails early if the mousedown target is a button/input/label/a, preventing e.preventDefault() from suppressing click events on interactive elements in the drag handle area

[0.4.3] 2026-05-29
- overlay.js: settings now pop out as a separate floating panel to the left of the main panel instead of replacing the tracker list; popout is a fixed-position shadow-root-level div with a right-pointing speech-bubble tail; tracker list stays fully visible; popout closes on outside click or gear button re-click

[0.4.2] 2026-05-29
- overlay.js: added ⚙ settings button to panel header; clicking it swaps the tracker list for a settings view containing: per-site enable/disable toggle (hides widget when disabled, reads/writes via background messages) and a reset-position button (restores top-right default and clears chrome.storage.local entry); settings view closes when panel closes; gear icon turns amber when settings are open

[0.4.1] 2026-05-29
- overlay.js: added click-and-drag to reposition the widget anywhere on screen; pill and panel header both act as drag handles; 4px movement threshold distinguishes drag from click so toggle still works; cursor changes to grabbing during drag; tooltip hides during drag; position persisted to chrome.storage.local and restored on next page load with viewport clamping

[0.4.0] 2026-05-29
- data/trackers.json: expanded from 154 to 241 trackers (+87) - added full coverage of:
  Ad exchanges: Adform, BidSwitch, Amobee, DataXu/Roku OneView, Undertone/Perion, EMX Digital, RhythmOne, Zedo, LoopMe, AppLovin, InMobi, Digital Turbine, Unity Ads/IronSource, Kargo, Bombora, Carbon Ads, BuySellAds, Ezoic, Mediavine, Raptive/AdThrive
  Affiliate: Skimlinks, VigLink/Sovrn Commerce, Impact, CJ Affiliate, Rakuten Advertising, Awin, ShareASale, PartnerStack, TUNE/HasOffers
  Attribution: Singular, Rockerbox, Northbeam, Triple Whale
  Session recording: Smartlook, Glassbox, Quantum Metric, Decibel Insight, SessionCam, SessionStack
  Product analytics: Sprig, Appcues, WalkMe, Chameleon, UserGuiding, Oribi
  Marketing: Constant Contact, Campaign Monitor, Brevo/Sendinblue, Omnisend, Attentive, Postscript, Yotpo, Bazaarvoice, Trustpilot Widget, Klarna, Affirm, Afterpay, Nosto, Barilliance
  Support: LivePerson, Gorgias, Kustomer
  Social: SoundCloud, Twitch, Tumblr, Substack
  Surveys: Qualtrics, SurveyMonkey, Medallia, Typeform, Delighted, UserVoice
  Error monitoring: AppDynamics, Raygun, TrackJS, Airbrake
  B2B/other: ZoomInfo, iSpot.tv, Algolia Insights, Google Maps API, Mapbox, Stripe Radar, PayPal

[0.3.3] 2026-05-29
- overlay.js: tooltip restyled as speech bubble - ::before draws border-colored tail, ::after draws fill-colored tail on top, creating a right-pointing arrow that points back at the hovered tracker row; slightly larger padding, rounder corners, lighter text

[0.3.2] 2026-05-29
- overlay.js: fixed tooltip not appearing - root cause was overflow-y:auto on .cc-list clipping absolutely-positioned children; replaced CSS :hover tooltip with a single shared position:fixed tooltip appended to shadow root, shown/hidden via JS mouseenter/mouseleave with viewport-relative coordinates

[0.3.1] 2026-05-29
- overlay.js: tracker list now shows logo + name only; description moved to CSS tooltip that appears to the left on hover; items vertically centered; cursor changed to help

[0.3.0] 2026-05-29
- overlay.js: moved pill from bottom-right to top-right (top: 20px); panel now drops down below pill
- data/trackers.json: expanded from 120 to 157 trackers - added Teads, Unruly, Tremor Video, TripleLift, Sharethrough, Nativo, Connatix, Innovid, Yahoo/Oath DSP, Quora Pixel, Wistia, Vidyard, JW Player, Brightcove, Leadfeeder/Dealfront, Albacross, Clearbit Reveal, Demandbase, 6sense, Adobe Analytics/Omniture, Adobe Audience Manager, Neustar/TransUnion, Acxiom, Perfect Audience, Salesloft, Outreach.io, Samba TV, GoSquared, etracker, Histats, Yieldmo, Smart AdServer/Equativ, Extreme Reach, Pubstack, Seedtag, Fathom Analytics, Simple Analytics, Plausible

[0.2.0] 2026-05-29
- overlay.js: floating pill injected into every page via Shadow DOM (style-isolated); shows live tracker count badge; click to expand panel with full tracker list, category badges, plain-English descriptions, and banner status; collapses on outside click or X button; receives push updates from background via TRACKER_UPDATE / BANNER_STATUS_UPDATE messages; requests initial state on load via GET_MY_TAB_DATA; respects per-site disable toggle
- background.js: added GET_MY_TAB_DATA handler (sender.tab.id context); added schedulePush() to debounce-push TRACKER_UPDATE to content scripts when new trackers are detected (400ms debounce, only on new trackers); BANNER_STATUS now also pushes BANNER_STATUS_UPDATE to overlay
- manifest.json: bumped to v0.2.0; added overlay.js to content_scripts
- data/trackers.json: expanded from 50 to 120 trackers - added Xandr/AppNexus, OpenX, Magnite/Rubicon, Index Exchange, PubMatic, Sovrn, The Trade Desk, MediaMath, Lotame, Oracle BlueKai, AdRoll, Moat, IAS, DoubleVerify, Kochava, Adjust, AppsFlyer, Branch, Conversant, GumGum, Revcontent, MGID, 33Across, SpotX, FreeWheel, ID5, LiveIntent, Audigent, Zeotap, Matomo, Woopra, Kissmetrics, Inspectlet, Clicky, StatCounter, Parse.ly, Pendo, Tealium, mParticle, PostHog, Contentsquare, AB Tasty, Dynamic Yield, Eloqua, ActiveCampaign, Mailchimp, Drip, Iterable, Emarsys, LiveChat, Freshchat, Olark, Tawk.to, Tidio, Crisp, Help Scout, YouTube Embeds, Vimeo Embeds, Twitter Widgets, Adobe Fonts, Spotify Embeds, Bugsnag, Rollbar, Dynatrace, Lucky Orange, Customer.io

[0.1.0] 2026-05-29
- Initial scaffold
- manifest.json (MV3) with webRequest + storage + tabs + scripting permissions
- background.js: service worker with webRequest interception, tracker domain matching, per-tab badge count, banner status storage, site enable/disable toggle
- content.js: cookie banner auto-decliner targeting OneTrust, Cookiebot, TrustArc, Osano, Termly, Didomi, Quantcast, Usercentrics, Consentmanager, CookieYes + generic fuzzy text matching; MutationObserver for late-loading banners
- popup/popup.html + popup.css + popup.js: dark-theme popup with tracker list (name, logo, category badge, plain-English description), banner status indicator, per-site toggle
- data/trackers.json: 50 trackers across advertising, analytics, social, marketing, support, performance categories
- icons: cookie-with-knife SVG + exported PNG at 16/48/128px

v1.0.0 (2026-06-01)
- Bumped version to 1.0.0 for Chrome Web Store launch
- Added privacy-policy.html (to host on GitHub Pages)
- Built submission zip: /tmp/trackertracker-1.0.0.zip (204KB, excludes scripts/, CHANGELOG.md, changelog.txt, icons/icon.svg)

v1.0.0 (2026-06-01) [patch]
- Replaced all em-dashes with hyphens across all project files

v1.0.0 (2026-06-01) [patch]
- Updated store description to credit Ghostery TrackerDB
- Created store-submission/ folder with zip, privacy-policy, screenshots, store-listing.txt

v1.0.3 (2026-06-01) [patch]
- background.js: removed stylesheet and font from BLOCK_RESOURCE_TYPES - trackers don't need these resource types, and blocking them caused sites to break when CDN domains (e.g. fonts.googleapis.com) appeared in the tracker list

v1.0.3 (2026-06-01) [patch]
- background.js: excluded support-category trackers from block-all and block-by-default - support tools (GitHub, Intercom, Zendesk, etc.) are often load-critical for site functionality; blocking them was causing hangs (e.g. GitHub dashboard stuck on "One moment please...")

v1.0.3 (2026-06-01) [patch]
- background.js: extracted BLOCK_ALL_EXCLUDED_CATS constant; added excludedCats to GET_BLOCK_ALL response so UI can reflect which categories are actually blocked
- overlay.js: added isEffectivelyBlocked() helper that respects excluded categories; support-category trackers no longer show as blocked/strikethrough when block-all is active; blocklist "blocked only" filter and toggle disabled state also updated

v1.0.3 (2026-06-01) [patch]
- background.js: fixed stale DNR rules causing block-all to stay active after being toggled off - root cause was atomic updateDynamicRules calls rolling back the removal when the add failed; split into separate remove/add calls so cleanup always succeeds; also restructured init() to unconditionally clear block-all rules before restoring state

v1.0.3 (2026-06-02) [fix 1/8]
- background.js: replaced hash-based domainToRuleId (birthday paradox ~64% collision chance with 10k+ domains) with sequential ID counter + persisted ruleIdMap; IDs are now stable across SW restarts and guaranteed unique; added saveRuleIdMap(), updated addBlockRules/removeBlockRules to use map, updated init() to restore map and seed nextIndividualRuleId from max existing ID

v1.0.3 (2026-06-02) [fix 2/8]
- background.js: wrapped addBlockRules and enableBlockAll calls in init() with try/catch - a failure in either no longer rejects initPromise, which would have silently broken all message handlers that depend on it

v1.0.3 (2026-06-02) [fix 3/8]
- background.js: added types filter to webRequest.onBeforeRequest listener - now only fires for script, xmlhttprequest, websocket, ping, sub_frame; trackers only phone home via these types so detection is unchanged, but listener no longer runs on every image/font/CSS request

v1.0.3 (2026-06-02) [fix 4/8]
- background.js: webRequest listener now checks details.initiator against disabledSites before doing anything; previously the badge would still increment and trackers would still be stored even when the extension was disabled for a site

v1.0.3 (2026-06-02) [fix 4/8]
- background.js: webRequest listener now checks details.initiator against disabledSites; badge no longer increments and trackers no longer stored for disabled sites
- overlay.js: overlay now hides itself on load if the site is disabled, not just when the toggle is changed mid-session

v1.0.3 (2026-06-02) [fix 4b/8]
- overlay.js: disabled sites no longer hide the pill entirely - pill stays visible but grayed out/desaturated; clicking it opens settings tab directly so the user can re-enable; fixes the lockout where disabling a site made the overlay inaccessible

v1.0.3 (2026-06-02) [fix 5/8]
- content.js: cached IS_SITE_DISABLED result in siteEnabledCache per page load; previously sent a background message on every MutationObserver debounce (every 300ms of DOM activity); the enabled state can't change mid-page so one check per load is sufficient

v1.0.3 (2026-06-02) [fix 6/8]
- overlay.js: replaced innerHTML with textContent for category header labels; eliminates XSS surface even though tracker data is currently bundled

v1.0.3 (2026-06-02) [fix 7/8]
- overlay.js: replaced e.target !== host with e.composedPath().includes(host) for correct shadow DOM click-outside detection; fixed didDrag never being reset after a drag ended, which caused click-outside-to-close to stop working after any drag

v1.0.3 (2026-06-02) [fix 8/8]
- popup/popup.js: added chrome.runtime.lastError checks to all sendMessage callbacks; fire-and-forget sends now use a shared suppressErr callback to prevent Chrome logging unchecked runtime error warnings when the SW is mid-restart

v1.0.3 (2026-06-02) [fix]
- overlay.js: panel now resets to tracker list tab when closed, so reopening always shows trackers not settings
- overlay.js: changed 'trackers on this page' to 'trackers on this page so far' to imply detection is ongoing

v1.0.3 (2026-06-02) [audit fixes]
- background.js: removeBlockRules now mutates domainRuleIds only after the DNR call succeeds; previously a failed updateDynamicRules would leave the rule active but the ID permanently lost from the map
- background.js: loadTrackers() failure now caught in init() so initPromise resolves instead of rejecting; extension degrades gracefully rather than breaking all message handlers
- background.js: BLOCK_TRACKER and UNBLOCK_TRACKER handlers now have .catch() - on failure, sendResponse({ ok: false }) is called and in-memory state is rolled back
- background.js: disableBlockAll() now has try/catch so blockAllEnabled state stays accurate on failure
- background.js: replaced Math.max(...Map.values()) with a for..of loop to avoid stack overflow when many trackers are individually blocked
- background.js: tabs.onUpdated now clears pushTimers on navigation to prevent stale debounced pushes firing on the new page
- background.js: GET_SITE_ENABLED, IS_SITE_DISABLED, GET_BLOCKED_TRACKERS now wait for initPromise before responding; fixes race condition where SW restart caused these to return stale empty data
- popup/popup.js: replaced innerHTML with textContent for category header labels

v1.0.3 (2026-06-02) [copy]
- overlay.js, popup/popup.html: updated 'Block all trackers' subtitle to 'Blocks all trackers except support' to reflect actual behavior
